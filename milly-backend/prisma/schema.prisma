generator client {
  provider     = "prisma-client"
  output       = "../src/generated/prisma"
  moduleFormat = "cjs"
}

datasource db {
  provider = "mysql"
}

model Ticket {
  id     Int    @id @default(autoincrement())
  title  String
  status String
}
